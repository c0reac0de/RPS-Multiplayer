//get only two players by setting inital values and limits

//start game once two players have joined

//user 1 and 2 input
    //store input
function rpsGame(player1Choice){
var p1Choice= player1Choice.id;
console.log(p1Choice);
}

    //send to firebase

//compare input to determine winner
    //use data if/else if (or switch case)

//output win/lose on scoreboard
    //connect firebase retrieved data to specified element